```js
        let num = parseInt(+prompt())
        function guestnum(mun){
            let freeee = Math.round(Math.random()*100) 
            // if(mun>freeee){
                
            // }
            while(mun<freeee){
                alert("太小了宝贝")
                mun = parseInt(+prompt(猜猜数字))
            }
            while(mun>freeee){
                alert("太大了宝贝")
                mun = parseInt(+prompt(猜猜数字))
            }
            while(mun=freeee){
                alert("小可爱真能猜")
                break
            }
        }
        console.log(guestnum(num));
```

