```js
var a= 1
var b= 3
function fn(){
		console.log(a)//undefined
		a=2
		console.log(a)//2
		var b=4
}
fn()
console.log(a) //2
console.log(b) //3
```

> ###### 声明形参相当于在函数中声明了对应的变量,但是没有赋值

```js                                                                                                                                                                                                                                      
var a= 1
var b= 3
function fn(a){
		console.log(a)//10
		a=2
		console.log(a)//2
		var b=4
}
fn(10)
console.log(a) //2
console.log(b) //3
```

```js
//现有两个数组 [12,6,8,9,45] [54,63,88,47,8,12] 请将两个数组合并为一个新的数组并去重
        let arr1 = [12,6,8,9,45]
        let arr2 = [54,63,88,47,8,12]
        function add(rra,rra1){
         let newarr = []
         for(let i = 0;i<rra1.length;i++){
            rra.push(rra1[i])
           
         }
         for(let j = 1;j<rra.length;j++){
             if(newarr.indexOf(rra[j])==-1){
                newarr.push(rra[j])
            }
         }
         return newarr
        }
        console.log(add([1,2,3,4],[9,8,7,6,5,4]));
        console.log(add(arr1,arr2));
        //创建一个自己的数学对象，里面具有PI属性和求最大值和最小值的两个方法
        let myMath = {
            PI:3.1415926,
            max:function (){
                let max1 = arguments[0]
                for(let i =1;i<arguments.length;i++){
                    if(max1<arguments[i]){
                        max1 = arguments[i]
                    }
                }
                return max1
            },
            
            min:function (){
                let min1 = arguments[0]
                for(let j = 1;j<arguments.length;j++){
                    if(min1>arguments[j]){
                        min1= arguments[i]
                    }
                }
                return min1
            }
        }
        // console.log();
        console.log(myMath.max(1,2,3,4,5));
        console.log(myMath.min(1,2,3,4,5,6,7));
        
        myMath.max(1,2,3,4,5)
        myMath.min(1,2,3,4,5,6,7)
        
        //将字符串IOS Android HTML5 Java 翻转
        let zfc = "IOS Android HTML5 Java"
        function turn(cfz){
            var newzfc = ""
            for(var i =cfz.length-1;i>=0;i--){
                newzfc +=cfz[i]
            }
            return newzfc
        }
        console.log(turn(zfc));
```

