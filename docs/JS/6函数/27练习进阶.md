*1、显示打印的日期。 格式为类似“XXXX年XX月XX日 星期X” 的当前的时间。*

2、计算出该班级的平均分（保留整数）。

 同学成绩数据如下

"小明:87; 小花:81; 小红:97; 小天:76;小张:74;小小:94;小西:90;小伍:76;小迪:64;小曼:76"*

```js
        1.随机产生一个十六进制的颜色值，封装成一个函数。
        function freecolor(){
            let qqq =["A","B","C","D","E","F"]
            let loc = []
            for(var num = 0;num<6;num++){//从第零位开始传入随机数
                loc[num] = Math.floor(Math.random()*16) 
                if(loc[num]>9){
                    let abc = Math.floor(Math.random()*qqq.length)
                    loc[num]= qqq[abc]  
                }
            }
            console.log(loc);
            return loc 
        }
        let colorforfree = freecolor()
        console.log(colorforfree.join(''));


        2.某班的成绩出来了，现在老师要把班级的成绩打印出来。
        // 方法一:对象创建班级名单
        var score = { 小明: 87, 小花: 81, 小红: 97, 小天: 76, 小张: 74, 小小: 94, 小西: 90, 小伍: 76, 小迪: 64, 小曼: 76 }
        function getDateStr(xcscore) {
            var d = new Date()
            var year = d.getFullYear()
            var month = d.getMonth()
            var date = d.getDate()
            var weekday = d.getDay()
            var hour = d.getHours()
            var minutes = d.getMinutes()
            var second = d.getSeconds()
            var week = ["星期天", "星期一", "星期二", "星期三", "星期四", "星期五", "星期六"]
            var total = 0
            var count = 0
            for (var stuname in xcscore) {
                total += xcscore[stuname]
                count++
            }
            var average = total / count
            return `${year}年${month}月${date}日${hour}点${minutes}分${second}秒 ${week[weekday]}--班级平均分为:${average}`
        }
        console.log(getDateStr(score))
        // 方法二:字符串创建班级名单
        var scores = ["87", "81", "97", "76", "74", "94", "90", "76", "64", "76"];
        function getDateStr(xcscore) {
            var d = new Date()
            var year = d.getFullYear()
            var month = d.getMonth()
            var date = d.getDate()
            var weekday = d.getDay()
            var hour = d.getHours()
            var minutes = d.getMinutes()
            var second = d.getSeconds()
            var week = ["星期天", "星期一", "星期二", "星期三", "星期四", "星期五", "星期六"]
            var total = 0
            var count = 0
            for (let i = 0; i < xcscore.length; i++) {
                total += parseInt(xcscore[i])
                count++
            }
            let average = total / count
            return `${year}年${month}月${date}日${hour}点${minutes}分${second}秒 ${week[weekday]}--班级平均分为:${average}`
        }
        console.log(getDateStr(scores))

```

