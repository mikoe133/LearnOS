```js
<body>
    <div>
        请选择你的爱好:
        <input type="checkbox" id="check-all" name="" >
        全选
    </div>
    <div>
        <input type="checkbox" name="hobby" value="乒乓球" id="">乒乓球
        <input type="checkbox" name="hobby" value="篮球" id="">篮球
        <input type="checkbox" name="hobby" value="羽毛球" id="">羽毛球
        <input type="checkbox" name="hobby" value="足球" id="">足球
    </div>
    <div>
        <button type="button" id="all">全选</button>
        <button type="button" id="no">取消</button>
        <button type="button" id="reverse">反选</button>
        <button type="button" id="send">提交</button>
    </div>

</body>
<script>
    window.onload= function (){
        // 全选功能
        //获取四个多选框
        const hobbies = document.getElementsByName('hobby')
        //获取全选按钮
        const allBtn = document.getElementById('all')
        allBtn.addEventListener("click",function (){
        //多选框变为选中
        for(let i = 0 ;i<hobbies.length;i++){
            hobbies[i].checked = true
        }
        checkall.checked = true
        })
        //取消功能
        //获取取消按钮
        const no = document.getElementById('no')
        no.addEventListener("click",function (){
            for(let i = 0;i<hobbies.length;i++){
                hobbies[i].checked = false
            }
            checkall.checked = false
        })
        //反选功能
        const reverse = document.getElementById('reverse')
        reverse.addEventListener("click",function (){
            for(let i = 0;i<hobbies.length;i++){
                hobbies[i].checked = !hobbies[i].checked 
            }
            if(hobbies.length==checkedbox.length){
                    checkall.checked = true
                }else{
                    checkall.checked = false
                }
        })
        //提交按钮
        const send = document.getElementById('send')
        send.onclick = function (){
            for(let i=0;i<hobbies.length;i++){
                if(hobbies[i].checked){
                    alert(hobbies[i].value)
                }
            }
        }
        //全选checkbox同步
        const checkall = document.getElementById('check-all')
        checkall.addEventListener("change",function (){
            for(let i = 0;i<hobbies.length;i++){
                hobbies[i].checked = this.checked
            }
        })
        //checkbox与全选同步
        for(let i = 0;i<hobbies.length;i++){
            hobbies[i].onchange= function (){
                const checkedbox = document.querySelectorAll('[name=hobby]:checked')
                if(hobbies.length==checkedbox.length){
                    checkall.checked = true
                }else{
                    checkall.checked = false
                }
            }
        }
    }
</script>
```

