#### clone() 用来复制jQuery对象,参数可以传true,则会复制对象上的事件

```js
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://lf26-cdn-tos.bytecdntp.com/cdn/expire-1-M/jquery/3.6.0/jquery.min.js"></script>
    <script>
        $(function (){
            var $swk = $("#list li:nth-child(1)").clone()
            //写了clone方法后,本来是添加后list中的孙悟空会消失,现在克隆了一个出来
            var $list2 = $("#list2")
            $("#btn").click(function (){
                $list2.append($swk)
            })
        })
    </script>
</head>
<body>
    <button id="btn">dian</button>
    <ul id="list">
        <li>孙悟空</li>
        <li>猪八戒</li>
    </ul>
</body>
    <ul id="list2">
        <li>萨达</li>
        <li>递归</li>
    </ul>
</body>
</html>
```

#### empty()删除所有元素

#### remove()移除所有元素(删除事件)

#### detach()移除元素

#### attr()

#### prop()

- 用来读取或设置元素的属性
- 读取布尔值属性时,会返回不同值
  - attr读取时返回实际值
  - prop返回true/false

添加和删除元素的方法
介绍了添加子元素的方法，包括往前面和往后面添加子元素。
讲解了添加虚拟元素的方法，如after和before。
演示了使用after和before方法添加元素的例子。
介绍了删除元素的方法，包括remove和empty。
示范了使用empty方法清空元素和使用remove方法移除元素的效果。

删除元素的方法和替换元素的方法
remove和detach都是移除元素，但remove彻底删除，detach暂时移除
detach不会删除元素上的事件，而remove会同时删除元素和事件
replace方法可以替换所有元素或指定元素

JavaScript属性操作方法和替换调用方式
JavaScript中的替换调用方式是通过replace with实现的
属性操作方法attr和prop用于读取或设置元素的属性

读取和移除属性的方法
内部属性
读取属性时，推荐使用prop方法，可以直接传递布尔值
使用attr方法读取布尔值时，需要判断属性是否存在
使用prop方法读取布尔值时，返回的是true或false
推荐使用prop方法，因为使用attr方法需要进行判断
removeAttr和removeProp方法可以移除属性

属性操作使用的方式和修改value的方式
可以对属性进行移除
y6 vl是专门设置y6属性的
通过prop去修改value属性
